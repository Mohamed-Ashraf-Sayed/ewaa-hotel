{
  "name": "ewaa-hotel-crm",
  "version": "1.0.0",
  "description": "Ewaa Hotels CRM",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate && npx prisma db push --accept-data-loss",
    "start": "cd backend && node src/app.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
